/*repeticao controlada por contador com a estrutura for*/
#include <stdio.h>
main()
{
int contador;
/*inicializacao, condicao de repetica e incremento*/ 
/*estao incluidos no cabecalho da estrutura */
for (contador = 1; contador <= 10; contador++)
        printf("%d\n", contador);
return 0;
}
