#include <stdio.h>
main ()
{
	float media; /* novo tipo de dado*/
	int contador, grau, total;
	/* fase de inicializaçcao */
	total = 0; contador = 0;
	/* fase de processamento */
	printf("entre com o grau, -1 para finalizar: ");
	scanf("%d", &grau);
	while (grau != -1) {
			total = total + grau; contador = contador + 1;
			printf("entre com o grau, -1 para finalizar: ");
			scanf ("%d", &grau);
			}
	/*fase de terminacao*/
	if (contador !=0) {
			media = (float) total / contador;
			printf ("A media da turma e %.2f\n", media); }
	else 
			printf("nenhum grau foi fornecido\n");

	return 0;  /*indica que o programa terminou corretamente */
	}
